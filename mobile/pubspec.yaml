name: mobile
description: An app integrating with Google Maps, from where people can connect and get help from the neighbour easily

publish_to: 'none'

version: 0.0.0-pilot+0

environment:
  sdk: '>=2.19.6 <3.0.0'

dependencies:
  flutter:
    sdk: flutter
  cupertino_icons: ^1.0.2

dev_dependencies:
  flutter_test:
    sdk: flutter
  flutter_lints: ^2.0.0

flutter:
  uses-material-design: true
  assets:
    - assets/anim/
    - assets/img/
